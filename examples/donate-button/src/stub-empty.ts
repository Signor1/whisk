/**
 * Stub for transitive deps that wagmi pulls but never executes in the
 * browser. Re-exported as `{}` so any property access yields undefined
 * cleanly without throwing.
 */
export default {};
export const noop = () => {};
