/**
 * Tailwind v4 uses the official PostCSS plugin — no `tailwind.config.js`
 * needed; theme configuration lives directly in `globals.css` via the
 * `@theme` block.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
